<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.clxmm.service.edu.mapper.CourseMapper">


    <sql id="columns">
        c.id,
        c.title,
        c.lesson_num AS lessonNum,
        t.NAME AS teacherName,
        c.cover,
        c.buy_count AS buyCount,
        c.view_count AS viewCount,
        c.STATUS,
        c.gmt_create AS gntCreate,
        c.price,
        s.title AS subjectTitle,
        s2.title AS subjectParentTitle
    </sql>

    <sql id="tables">
        edu_course c
        LEFT JOIN edu_teacher t ON c.teacher_id = t.id
        LEFT JOIN edu_subject s ON c.subject_parent_id = s.id
        LEFT JOIN edu_subject s2 ON c.subject_id = s2.id
    </sql>


    <select id="selectPageByCourseQueryVo" resultType="org.clxmm.service.edu.entity.vo.CourseVo">
        SELECT
            <include refid="columns"></include>
        FROM
            <include refid="tables"></include>
        ${ew.customSqlSegment}
    </select>
</mapper>
